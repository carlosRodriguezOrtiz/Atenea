{% extends 'base.html.twig' %}

{% block title %}Ejemplo DAFO{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    table,td,tr,th {
        border: 1px solid black;
    }
</style>
<div class="example-wrapper">
    {% for sup in supervivencia %}
        {{ sup[0].getQuestioInterna().getNombre() }} x {{ sup[1].getQuestioExterna().getNombre() }} <br>
    {% endfor %}
    <br>
    {% for of in ofensiva %}
        {{ of[0].getQuestioInterna().getNombre() }} x {{ of[1].getQuestioExterna().getNombre() }} <br>
    {% endfor %}
    <br>
    {% for re in reorientacion %}
        {{ re[0].getQuestioInterna().getNombre() }} x {{ re[1].getQuestioExterna().getNombre() }} <br>
    {% endfor %}
    <br>
    {% for de in defensiva %}
        {{ de[0].getQuestioInterna().getNombre() }} x {{ de[1].getQuestioExterna().getNombre() }} <br>
    {% endfor %}

</div>
{% endblock %}
