{% extends 'base.html.twig' %}

{% block title %}Hello FactorCriticoDeExitoController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <table class="table table-responsive-sm">
                <section class="row ">
                <div class="col-md-8"></div>
                <div class="col-md-4">
                    <div class="btn-group float-right mt-2 mb-3" role="group">
                        <a class="btn btn-danger btn-md" href="{{ path('inicio') }}">&laquo; Volver</a>
                    </div>
                </div>
            </section>
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Descripción</th>
                               <!-- <th scope="col">Nombre</th>-->

                            </tr>
                        </thead>
                        <tbody>                
                            {% for fce in factores %}                   
                                <tr>
                                    <th scope="row">{{fce.id}}</th>
                                    <td  scope="row"> {{ fce.getDescripcion() }}</th>
                                  <!--   <td  scope="row"> {{ fce.getNombre() }}</th>-->
                                </tr>
                            {% endfor %}
                            </table>
</div>
{% endblock %}
