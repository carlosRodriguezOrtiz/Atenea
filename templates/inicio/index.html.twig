{% extends 'base.html.twig' %}
{% block title %}Inicio
{% endblock %}
{% block body %}
    <section class="bg-white text-black text-center">
        <section class="container ">
            <section class="row text-center">
                <section class="col-md-12 mt-5">
                <a class="btn btn-primary btn-lg" href="{{ path('corporaciones_new') }}">Crear corporación</a>
                <a class="btn btn-secondary btn-lg"  href="{{ path('empresas_new') }}">Crear empresa</a> 
                </section>
            </section>
        </section>
    </section>
{% endblock %}